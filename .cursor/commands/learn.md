Learn one fact from the current game or from the user's description. Follow the `btd6-knowledge` and `btd6-live-screen` skills.

If the user did not name the fact, ask which control or rule failed. Search the knowledge store first. Update only that entry. Click the live game only when the user says it is open, and only once before checking the result.
